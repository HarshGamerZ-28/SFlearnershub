// lib/config.ts — Config variables for SF Learners Hub
// IMPORTANT: Set to true for local development (backend CORS issues)
// Set to false when backend CORS is properly configured
export const USE_MOCK_DATA = true;
